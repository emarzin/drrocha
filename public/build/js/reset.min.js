<<<<<<< HEAD
$(document).ready(function(){function setCredentials(data){storage.set({user:data.user}),storage.set({token:data.token}),$.ajaxSetup({headers:{Authorization:data.token}})}var storage=Storages.localStorage;$("#btnResetPassSpinner").hide(),$("#btnResetPassLabel").text("Cadastrar Nova Senha"),$("#btnResetPass").click(function(){var errEmail=$("#errEmail");errEmail.text("");var password=$("#password").val(),confirmPassword=$("#confirmPassword").val();if(""!==password&&""!==confirmPassword)if(password===confirmPassword){$("#btnResetPass").prop("disabled",!0),$("#btnResetPassLabel").text("Processando"),$("#btnResetPassSpinner").show();var data={token:$("#token").text(),password:password,confirmPassword:confirmPassword},resetPass=$.ajax({url:"/api/reset",type:"POST",data:data});resetPass.done(function(data){errEmail.text("Senha atualizada com sucesso! Aguarde..."),$("#btnResetPass").hide(),setCredentials(data),window.location.href="/members?token="+data.token}),resetPass.fail(function(xhr,textStatus){errEmail.text(xhr.responseText),$("#btnResetPassSpinner").hide(),$("#btnResetPassLabel").text("Cadastrar Nova Senha"),$("#btnResetPass").prop("disabled",!1)})}else errEmail.text("A confirmação da senha não confere! Verifique!");else errEmail.text("Informe sua nova senha!")})});
=======
$(document).ready(function() {
    function setCredentials(data) {
        storage.set({
            user: data.user
        }), storage.set({
            token: data.token
        }), $.ajaxSetup({
            headers: {
                Authorization: data.token
            }
        })
    }
    var storage = Storages.localStorage;
    $("#btnResetPassSpinner").hide(), $("#btnResetPassLabel").text("Cadastrar Nova Senha"), $("#btnResetPass").click(function() {
        var errEmail = $("#errEmail");
        errEmail.text("");
        var password = $("#password").val(),
            confirmPassword = $("#confirmPassword").val();
        if ("" !== password && "" !== confirmPassword)
            if (password === confirmPassword) {
                $("#btnResetPass").prop("disabled", !0), $("#btnResetPassLabel").text("Processando"), $("#btnResetPassSpinner").show();
                var data = {
                        token: $("#token").text(),
                        password: password,
                        confirmPassword: confirmPassword
                    },
                    resetPass = $.ajax({
                        url: "/api/reset",
                        type: "POST",
                        data: data
                    });
                resetPass.done(function(data) {
                    errEmail.text("Senha atualizada com sucesso! Aguarde..."), $("#btnResetPass").hide(), setCredentials(data), window.location.href = "/members?token=" + data.token
                }), resetPass.fail(function(xhr, textStatus) {
                    errEmail.text(xhr.responseText), $("#btnResetPassSpinner").hide(), $("#btnResetPassLabel").text("Cadastrar Nova Senha"), $("#btnResetPass").prop("disabled", !1)
                })
            } else errEmail.text("A confirmação da senha não confere! Verifique!");
        else errEmail.text("Informe sua nova senha!")
    })
});
>>>>>>> novas logomarcas produtos
